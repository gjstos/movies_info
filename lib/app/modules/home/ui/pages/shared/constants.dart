import 'package:flutter/material.dart';

final defaultShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.circular(15),
);

final cardColor = Colors.grey[850];

final defaultTooltipDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.circular(15),
);

final defaultTooltipTextStyle = TextStyle(color: Colors.black);
